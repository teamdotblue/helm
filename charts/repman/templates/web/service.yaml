apiVersion: v1
kind: Service
metadata:
  name: {{ template "repman.fullname" . }}-web
  labels:
    app: {{ template "repman.name" . }}-web
spec:
  clusterIP: None
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  selector:
    app: {{ template "repman.name" . }}-web
